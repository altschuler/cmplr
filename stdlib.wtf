# binary operators
op > 10 (l r) r < l;
op | 5 (l r) if l then 1 else if r then 1 else 0;
op & 5 (l r) if l then if r then 1 else 0 else 0;
op = 4 (l r) if l < r then 0 else if r < l then 0 else 1;
op : 4 (l r) r;

# unary operators
op - 4 (e) 0 - e;
op ! 4 (e) if e then 0 else 1;

# some useful functions
extern sin(a);
extern cos(a);
extern tan(a);

extern pchar(c);
extern pdoub(n);
extern pline();

extern exit();

# utility
def plines(n) 
	for i = 0, i < (n - 1) in pline();