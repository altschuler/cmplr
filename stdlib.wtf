# binary operators
op > 10 (l r) r < l;
op | 5 (l r) if l then 1 else if r then 1 else 0;
op & 5 (l r) if l then if r then 1 else 0 else 0;
op = 4 (l r) if l < r then 0 else if r < l then 0 else 1;

# unary operators
op - 1 (e) 0 - e;
op ! 1 (e) if e then 0 else 1;

# some useful functions
extern sin(a);
extern cos(a);
extern tan(a);

extern print();
extern exit();